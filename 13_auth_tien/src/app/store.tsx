import { configureStore } from '@reduxjs/toolkit';
import asadsadsadsadsadds from '../redux/silce/navbarSlice';

export const store = configureStore({
  reducer: {
		navbar: asadsadsadsadsadds,
  }
});